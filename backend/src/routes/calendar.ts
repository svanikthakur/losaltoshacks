/**
 * Calendar push — fundraising milestone generation + Google Calendar export.
 * Phase 4; stub now.
 */
import { Router } from 'express'

const router = Router()

router.post('/push', (_req, res) => {
  res.status(501).json({ error: 'Phase 4: calendar push not yet implemented' })
})

export default router
