# Server
PORT=4000
JWT_SECRET=replace-me-with-a-long-random-string

# External APIs (all optional in stub mode — pipeline runs mocked when unset)
GEMINI_API_KEY=
GITHUB_TOKEN=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MAILGUN_API_KEY=

# Supabase (optional — backend currently uses an in-memory store)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
